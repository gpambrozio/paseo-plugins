<!-- Sent to the first mate when watch scripts (watches/ in the home) have printed something. {{watches}} is one block per run — everything that run printed — oldest first, from watch-output.md or watch-failed.md, after watch-dropped.md when the queue overflowed. Only structure and the plugin's own facts go here: how a run's output is to be read is for the script to say, in its output. -->

<firstmate-watches>
{{watches}}
</firstmate-watches>
