<!-- One run's output in a watch message: the blocks go one after another, oldest first, with nothing of the plugin's around them — how the output is to be read is the script's to say. {{name}} is the script's file name in watches/, {{ran}} when it ran (UTC), {{output}} what it printed. -->

<firstmate-watch name="{{name}}" ran="{{ran}}">
{{output}}
</firstmate-watch>
